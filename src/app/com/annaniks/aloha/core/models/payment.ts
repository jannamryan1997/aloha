export interface BillingdetailsResponse {
    id: string;
    recv: boolean;
    pay: boolean;
    details: string;
}
export interface BillingdetailsData{
    recv: boolean;
    pay: boolean;
    details: string;
}